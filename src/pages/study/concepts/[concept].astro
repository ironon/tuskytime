---
import Layout from "../../../layouts/default.vue";
import {ConceptEdit} from "../../../components/ConceptEdit"
let conceptName = Astro.params.concept

---

<Layout client:only="vue" minimizable={true}>
  <ConceptEdit client:only="react" conceptName={conceptName}/>
</Layout>

